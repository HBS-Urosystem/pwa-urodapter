# Production site URL (no trailing slash). Used for Open Graph, canonical URLs, sitemap, and robots.txt.
# Set this in Netlify environment variables for deploys.
PUBLIC_SITE_URL=https://app.urodapter.com
